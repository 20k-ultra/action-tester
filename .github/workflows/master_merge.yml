name: "balenaCloud"
on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: "Deploy release"
    steps:
        name: "Deploy release"
        id: deploy
        uses: 20k-ultra/deploy-release@v5